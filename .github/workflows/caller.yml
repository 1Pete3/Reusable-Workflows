name: caller
run-name: caller
on:
    workflow_dispatch:
    pull_request:
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/main.yml
    with:
      config-path: ./.github/labeler.yml
    secrets: inherit