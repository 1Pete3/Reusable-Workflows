name: caller
run-name: caller
on:
    workflow_dispatch:
    pull_request_target:
        types: [opened]
        branches:
            - main
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/main.yml
    with:
      config-path: .github/labeler.yml
    secrets: inherit